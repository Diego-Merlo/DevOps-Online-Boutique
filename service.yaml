apiVersion: v1
kind: Service
metadata:
  name: recommendation-service
  namespace: default
spec:
  selector:
    app: recommendation
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP